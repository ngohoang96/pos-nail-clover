import ModalManagerSystemScreen from './ModalManagerSystemScreen'
import { connect } from 'react-redux'

const mapStateToProps = (state) => ({

})

const mapDispatchToProps = dispatch => {
    return {

    }
}

export default connect(mapStateToProps, mapDispatchToProps)(ModalManagerSystemScreen)
